# Company Platform Monorepo

Enterprise monorepo setup using Vue 3, TypeScript, Vite, and Turborepo. The repository hosts a shell application and example micro-apps with shared packages for UI, API, authentication, and common utilities.

## Getting Started

```bash
pnpm install
pnpm run dev:shell
```

Additional commands and structure are documented in the project files.
